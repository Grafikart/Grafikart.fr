{% extends 'admin/layout/card.html.twig' %}

{% block title %}
  {{ icon('comments') }}
  Gestion des tags
{% endblock %}

{% block actions %}
  <a href="{{ path(prefix ~ '_new') }}" class="btn-primary">Ajouter un tag</a>
{% endblock %}

{% block content %}
  <item-sorter items="{{ tags }}" endpoint="{{ path('admin_forum-tag_positions') }}"></item-sorter>
{% endblock %}
