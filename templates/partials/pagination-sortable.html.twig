<a{% for attr, value  in options %} {{ attr }}="{{ value }}"{% endfor %}>
{{ title }}
{% if sorted %}
  {% if direction == 'desc' %}
    {{ icon('sort') }}
  {% else %}
    {{ icon('sort') }}
  {% endif %}
{% endif %}
</a>
