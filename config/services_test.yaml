parameters:
  admin_prefix: '%env(resolve:ADMIN_PREFIX)%'
  download_path: '%kernel.project_dir%/tests/fixtures'
  videos_path: '%download_path%'
  sources_path: '%download_path%'

services:
  App\Tests\Infrastructure\Mercure\PublisherStub:
    class: App\Tests\Infrastructure\Mercure\PublisherStub
    decorates: mercure.hub.default.publisher
