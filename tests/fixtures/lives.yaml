include:
  - ../../fixtures/templates.yaml

App\Domain\Live\Live:
  live{1..10} (extends live): ~
  latest-live (extends live):
    name: Last Live
    created_at: <dateTimeBetween("first day of next year", "first day of next year")>
