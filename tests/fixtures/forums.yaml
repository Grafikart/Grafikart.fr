include:
  - ../../fixtures/templates.yaml

App\Domain\Forum\Entity\Category:
  category{1..4} (extends category):

App\Domain\Forum\Entity\Forum:
  forum{1..2} (extends forum):
    category: '@category1'
  forum{3..4} (extends forum):
    category: '@category2'
  forum{5..6} (extends forum):
    category: '@category3'
  forum{7..10} (extends forum):
    category: '@category4'
